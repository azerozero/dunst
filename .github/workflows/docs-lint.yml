# Docs lint pipeline: markdownlint + lychee.
#
# The local Python checker stays in the main CI as a fast offline guard for
# repo-local links. This workflow adds prose-shape checks and live link
# validation for documentation changes.

name: Docs Lint

on:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - "**.md"
      - ".markdownlint.json"
      - ".lycheeignore"
      - ".github/workflows/docs-lint.yml"
  pull_request:
    branches: [main]
    paths:
      - "docs/**"
      - "**.md"
      - ".markdownlint.json"
      - ".lycheeignore"
      - ".github/workflows/docs-lint.yml"

concurrency:
  group: docs-lint-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  markdownlint:
    name: markdownlint
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Run markdownlint-cli2
        uses: DavidAnson/markdownlint-cli2-action@v20
        with:
          globs: |
            docs/**/*.md
            README.md
          config: .markdownlint.json

  lychee:
    name: lychee
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Restore lychee cache
        uses: actions/cache@v4
        with:
          path: .lycheecache
          key: cache-lychee-${{ github.sha }}
          restore-keys: cache-lychee-
      - name: Run lychee
        uses: lycheeverse/lychee-action@v2
        with:
          args: >-
            --cache
            --max-cache-age 1d
            --no-progress
            --accept 200,204,206,301,302,303,307,308,403,429
            docs/**/*.md README.md
          fail: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
