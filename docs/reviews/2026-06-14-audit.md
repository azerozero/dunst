# Cycle - dunst-mcp

Date: 2026-06-14
Scope: full project
Language: English

## Scorecard

| Area | Score | Status | Skill |
|---|---:|---|---|
| Code quality | 5.9/10 | Acceptable, safety-boundary debt | cli-audit-code |
| Semantic X-Ray | 6.1/10 | Raw-action semantics and erased failures | cli-audit-xray |
| Shell quality | 7.6/10 | Good, wrapper drift remains | cli-audit-shell |
| Doc-code sync | 4.8/10 | Failing semantic sync | cli-audit-sync |
| Documentation | 5.6/10 | Acceptable, stale/current split missing | cli-audit-doc |
| Test quality | 61.25/100 | Managed, missing live/coverage/perf gates | cli-audit-test |
| Code topology | 72/100 | Acceptable, central hubs | cli-audit-tangle |
| Semantic drift | 6.8/10 | Drift detected | cli-audit-drift |
| README | 4.8/10 | Needs work | cli-forge-readme |
| Project structure | 8.4/10 | Clean Rust workspace | cli-forge-tree |
| Diagrams | 5.8/10 | Missing maintained architecture diagram | cli-forge-schema |
| Pipeline | 8/60 | Minimal CI only | cli-forge-pipeline |
| GitHub health | 52/100 | Local audit only, CI untracked | cli-forge-github |
| Setup/wizard UX | 45/100 | Rough setup lifecycle | cli-audit-wizard |
| Overall | 5.73/10 | Needs hardening | cli-cycle |

## Tier 3 - Critical (4 items)

| # | Correction | Effort | Source |
|---:|---|---|---|
| 1 | Gate or explicitly re-contract raw mutating input tools. `click_at`, `double_click_at`, `press_key`, `type_keys`, `scroll`, `zoom`, and `hotkey` bypass element-level risk semantics while `docs/CONTRACTS.md` says the gate is never bypassed. | Medium | cli-audit-code, cli-audit-drift, cli-audit-xray, cli-audit-sync, cli-audit-test |
| 2 | Split `approve` from the normal action tool surface, or document that it is not a security boundary. The same MCP client can call `approve` and then execute the gated action. | Medium | cli-audit-code, cli-audit-drift |
| 3 | Stop building AppleScript source with the raw `close_app` app name. Pass the value as data/argv or resolve by trusted pid/window identity. | Low | cli-audit-code, cli-audit-xray |
| 4 | Fix the live smoke server-name assertion: `scripts/smoke-live.py` still expects `visualops-mcp`, while the server now advertises `dunst`. | Low | cli-audit-sync |

## Tier 2 - Major (31 items)

| # | Correction | Effort | Source |
|---:|---|---|---|
| 5 | Reject malformed `read_series` points, or preserve per-point errors so request/result cardinality cannot silently change. | Low | cli-audit-code, cli-audit-drift, cli-audit-xray |
| 6 | Preserve OCR/capture failure channels instead of collapsing fovea/chart failures into empty results. | Low | cli-audit-code, cli-audit-xray |
| 7 | Replace predictable `/tmp` capture filenames with unique temp files plus cleanup guards. | Low | cli-audit-xray |
| 8 | Do not mask failed live attach by silently serving the fixture in the default live wrapper path. | Low | cli-audit-wizard |
| 9 | Preserve diagnostics for `scripts/smoke-live.sh` app launch failures instead of `open -a "$APP" \|\| true`. | Low | cli-audit-shell |
| 10 | Add a setup/config lifecycle command or equivalent dry-run/apply/edit/migrate path for MCP client registration. | Medium | cli-audit-wizard |
| 11 | Make MCP registrations portable; current `.mcp.json` and `.codex/config.toml` hardcode `/Users/ludwig/workspace/...`. | Low | cli-audit-wizard, cli-audit-sync, cli-forge-tree |
| 12 | Expand `doctor` to check client config, wrapper path, MCP handshake/tool list, buildability, and target discovery. | Medium | cli-audit-wizard |
| 13 | Consolidate the overlapping MCP launchers and environment contracts between `mcp-dunst.sh` and `mcp-notes.sh`. | Medium | cli-audit-shell, cli-audit-wizard |
| 14 | Update README setup for Rust/macOS prerequisites, TCC permissions, `doctor`, expected success output, troubleshooting, and contributor workflow. | Medium | cli-forge-readme, cli-audit-doc, cli-audit-sync, cli-audit-wizard |
| 15 | Fix stale `visualops-*` crate names in architecture, contracts, work packages, reviews, scripts, and CI branch naming, or mark historical documents as historical. | Medium | cli-audit-doc, cli-audit-sync, cli-forge-tree, cli-forge-schema |
| 16 | Mark old audit/review documents as historical where they contradict current implementation, especially approval lifecycle findings now fixed in code. | Low | cli-audit-doc, cli-audit-sync |
| 17 | Add a docs index/current-vs-historical map so stale WP/audit docs do not look authoritative. | Medium | cli-audit-doc, cli-audit-sync, cli-forge-tree |
| 18 | Define a language policy for mixed English/French documentation. | Medium | cli-audit-doc |
| 19 | Add rustdoc summaries, `# Errors`, and examples for public MCP-facing `Result` APIs. | Medium | cli-audit-doc |
| 20 | Add a maintained architecture diagram and update diagram labels to the current `dunst-*` workspace. | Medium | cli-forge-schema, cli-audit-doc |
| 21 | Correct tool docs that say cursor borrow decouples the user's mouse when platform code deliberately does not decouple for hover routing. | Low | cli-audit-sync |
| 22 | Gate live stdio/macOS smoke behavior in CI or define an explicit scheduled/manual lane for it. | Medium | cli-audit-test, cli-forge-pipeline, cli-audit-sync |
| 23 | Add a coverage report and target for the contract-mapped invariants. | Medium | cli-audit-test, cli-forge-pipeline |
| 24 | Add property-based, fuzz, or mutation checks for semantic drift surfaces. | Medium | cli-audit-test, cli-forge-pipeline |
| 25 | Add explicit raw coordinate/keyboard safety regression tests. | Medium | cli-audit-test, cli-audit-drift |
| 26 | Give live smoke a disposable-state cleanup contract before using it as a hard gate. | Medium | cli-audit-test |
| 27 | Turn existing benchmarks into optional or scheduled regression gates with saved p50/p95 artifacts. | Medium | cli-audit-test, cli-audit-xray, cli-forge-pipeline |
| 28 | Avoid duplicated composited capture/OCR work in `scan_chart`; use one captured frame for OCR and curve extraction where possible. | Medium | cli-audit-xray |
| 29 | Add documentation gates: markdown lint, link check, spelling/prose lint, rustdoc, and doc-test strategy. | Medium | cli-audit-doc, cli-forge-pipeline |
| 30 | Track and commit the local CI workflow if it is meant to protect the repository; `.github/` is currently untracked. | Low | cli-forge-github |
| 31 | Harden the workflow with explicit `permissions:`, immutable or tighter action refs, cache, security/dependency/license/secret/SBOM checks, and CI YAML validation. | Medium | cli-forge-github, cli-forge-pipeline |
| 32 | Split fast/full CI lanes and add path pruning so docs-only or shell-only changes do not schedule the whole macOS Rust lane. | Medium | cli-forge-pipeline |
| 33 | Decide release automation scope; release flow is explicitly deferred and no release workflow exists. | High | cli-forge-github |
| 34 | Split large hubs: `dunst-mcp/src/engine.rs`, `dunst-platform/src/lib.rs`, and the `serve.rs` tool registry/dispatcher. | High | cli-audit-code, cli-audit-tangle, cli-audit-xray |
| 35 | Add typed tool descriptors so schema, parsing, execution, and serialization cannot drift independently. | Medium | cli-audit-code, cli-audit-tangle, cli-audit-xray |

## Tier 1 - Minor (19 items)

| # | Correction | Effort | Source |
|---:|---|---|---|
| 36 | Rework README opening so it leads with the user-facing outcome before POC/internal wording. | Low | cli-forge-readme |
| 37 | Add README badges and an explicit license section. | Low | cli-forge-readme |
| 38 | Add a terminal transcript, screenshot, GIF, or MCP result sample to show observable output. | Medium | cli-forge-readme |
| 39 | Add a beginner tutorial from clone to fixture demo to first live window interaction. | Medium | cli-audit-doc |
| 40 | Replace remaining "blacklist" terminology in docs. | Low | cli-audit-doc |
| 41 | Decide whether `.obsidian/` should be ignored or kept out of the working tree. | Low | cli-forge-tree |
| 42 | Add `--help`/usage handling to `mcp-notes.sh`. | Low | cli-audit-shell |
| 43 | Add `--help` handling to `smoke-live.sh` instead of treating `--help` as an app name. | Low | cli-audit-shell |
| 44 | Wrap `mcp-dunst.sh` top-level flow in `main "$@"` before further option growth. | Low | cli-audit-shell |
| 45 | Use lowercase names for mutable shell locals such as `BIN`, `APP`, and `PID`, if changing shell style is desired. | Low | cli-audit-shell |
| 46 | Add an explicit post-build executable check to `mcp-notes.sh`. | Low | cli-audit-shell |
| 47 | Document why Codex MCP startup timeout is `120s`. | Low | cli-audit-wizard |
| 48 | Add traceability/planned-vs-implemented marking to the existing Mermaid sequence diagram. | Low | cli-forge-schema |
| 49 | Shorten dense Mermaid sequence labels. | Low | cli-forge-schema |
| 50 | Add CI `timeout-minutes`, workflow concurrency, and selective retry controls. | Low | cli-forge-pipeline, cli-forge-github |
| 51 | Avoid installing ShellCheck through uncached `apt` on every CI run. | Low | cli-forge-pipeline |
| 52 | Prove the documented pure cross-platform path in CI, not only macOS. | Medium | cli-forge-pipeline |
| 53 | Add CODEOWNERS if ownership routing matters for review. | Low | cli-forge-github |
| 54 | Benchmark or remove risk-assessment per-node joined-string allocation and tune hardcoded UI/chart timing constants. | Medium | cli-audit-code, cli-audit-xray |

## Strengths

- Rust workspace boundaries are clean: core contracts, pure graph logic, platform backend, vision, and MCP transport are separated.
- The crate dependency graph is acyclic, sparse, and has no confirmed dead functions.
- Contract tests around element-bound risk gating, approvals, drag/type composite risk, scene projection, and coordinate transforms are strong.
- Shell scripts use strict mode, keep MCP stdout clean, quote variables consistently, and are ShellCheck-clean.
- CI already covers fmt, clippy with `-D warnings`, locked tests, build, and ShellCheck in the working tree.
- macOS FFI ownership and coordinate transform invariants are unusually well documented for a POC.

## Trends

No prior report was found under `docs/reviews/`, so this is the first baseline for the cycle trend table.

## Verification

- `cargo fmt --all -- --check`: passed
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`: passed
- `cargo test --workspace --all-targets --locked`: 72 tests passed

## Phoenix Status

Not converged. There are 4 critical and 31 major corrections remaining.

## Phoenix Choice

| Choice | Action | Items |
|---|---|---:|
| `1` | Fix everything (autonomous convergence, unified plan) | 54 |
| `2` | Fix Tier 3 critical only | 4 |
| `3` | Fix Tier 2 major only | 31 |
| `4` | Fix Tier 1 minor only | 19 |
| `5` | Not now | - |
